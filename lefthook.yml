# https://lefthook.dev/configuration/
#
pre-commit:
  parallel: true
  jobs:
    - name: lint
      run: golangci-lint run
    - name: go mod tidy
      run: go mod tidy -diff
    - name: build ts stubs
      run: bun run proto:generate
