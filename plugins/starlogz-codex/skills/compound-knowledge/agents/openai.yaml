interface:
  display_name: "Compound Knowledge"
  short_description: "Build durable project memory with Starlogz"
  default_prompt: "Use $compound-knowledge to recall relevant project context before starting this task."
dependencies:
  tools:
    - type: "mcp"
      value: "starlogz"
      description: "A user-configured Starlogz MCP server"
policy:
  allow_implicit_invocation: true
