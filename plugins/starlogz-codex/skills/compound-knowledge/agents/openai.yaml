interface:
  display_name: "Compound Knowledge"
  short_description: "Recall durable project context on demand"
  default_prompt: "Use $compound-knowledge to recall relevant project context and preserve one concise reusable result."
dependencies:
  tools:
    - type: "mcp"
      value: "starlogz"
      description: "A user-configured Starlogz MCP server"
policy:
  allow_implicit_invocation: false
